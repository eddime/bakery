{
  "name": "bakery",
  "version": "1.0.0",
  "type": "module",
  "bin": {
    "bake": "./bake"
  },
  "scripts": {
    "install": "bun link",
    "build:launchers": "bun scripts/build-launchers.ts",
    "download:binaries": "bun scripts/download-binaries.ts",
    "postinstall": "bun scripts/download-binaries.ts || true"
  },
  "dependencies": {
    "sharp": "^0.34.5"
  }
}
